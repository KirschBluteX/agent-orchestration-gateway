interface:
  display_name: "Agent Orchestration Gateway"
  short_description: "Supervise approved native task graphs without runtime state"
  default_prompt: "Use $agent-orchestration-gateway:orchestrate to clarify this initiative, propose a module DAG, and supervise it after I approve."

policy:
  allow_implicit_invocation: false
