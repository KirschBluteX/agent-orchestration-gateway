interface:
  display_name: "Codex Cost Orchestrator"
  short_description: "Route implementation through cost-aware verified lanes"
  default_prompt: "Use $orchestrate to choose the appropriate cost-aware route for this implementation and verify it with route-proportionate evidence."

policy:
  allow_implicit_invocation: true
