interface:
  display_name: "Codex Cost Orchestrator"
  short_description: "Route closed native Agent work with static local policy"
  default_prompt: "Keep unresolved work in Primary; derive role, assurance, placement, and a static local route for each closed node, then verify acceptance IDs against the exact resulting state."

policy:
  allow_implicit_invocation: true
