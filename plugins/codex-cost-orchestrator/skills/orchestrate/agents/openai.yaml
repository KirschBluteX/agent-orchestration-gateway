interface:
  display_name: "Codex Cost Orchestrator"
  short_description: "Route closed native Agent work with static local policy"
  default_prompt: "Keep unresolved work in Primary; make one graph-level decision with no per-node model-classifier request, route the ready graph once, dispatch closed capsules in the same turn, wait for authoritative native events, and verify acceptance IDs against the exact resulting state."

policy:
  allow_implicit_invocation: true
